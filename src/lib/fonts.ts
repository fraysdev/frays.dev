import { Geist_Mono, Lexend } from "next/font/google";

export const lexendMedium = Lexend({
  weight: "500",
  subsets: ["latin"],
});

export const lexendLight = Lexend({
  weight: "300",
  subsets: ["latin"],
});

export const geistMono = Geist_Mono({
  weight: "500",
  subsets: ["latin"],
});
